node_modules/
.aws-sam/
dist/

# Local + deploy config that contain secrets — keep only the *.example.* versions
env.json
samconfig.toml
.env
.env.*

# Supabase CLI local state (keep config.toml, migrations/, seed.sql)
supabase/.temp/
supabase/.branches/

*.log
.DS_Store
